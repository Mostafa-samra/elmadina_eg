import 'package:flutter/material.dart';

import 'widget/callus_body.dart';

class CallUs extends StatelessWidget {
  const CallUs({super.key});

  @override
  Widget build(BuildContext context) {
    return const CallUsViewBody();
  }
}

import 'package:flutter/material.dart';

import 'widget/projects_view_body.dart';

class Projects extends StatelessWidget {
  const Projects({super.key});

  @override
  Widget build(BuildContext context) {
    return const ProjectsViewBody();
  }
}

